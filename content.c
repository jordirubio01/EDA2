#include <malloc.h>
#include "content.h"
#include "string.h"

/*ActivityLinked* make_activity_linked(char name[MAX_LENGTH], char type[MAX_LENGTH], char location[MAX_LENGTH], char schedule[MAX_LENGTH], char review[MAX_LENGTH_REVIEW], int stars, double price, User* user, ActivityLinked* first){
    ActivityLinked* c = (ActivityLinked*) malloc(sizeof(ActivityLinked));
    strcpy(c->activity->name, name);
    strcpy(c->activity->type, type);
    strcpy(c->activity->location, location);
    strcpy(c->activity->schedule);
    strcpy(c->activity->review);
    c->activity->stars = stars
    c->activity->price = price
    c->activity->user = user

}*/